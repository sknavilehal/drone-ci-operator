---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: droneservers.drone.ocean.dev
spec:
  group: drone.ocean.dev
  names:
    kind: DroneServer
    listKind: DroneServerList
    plural: droneservers
    singular: droneserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DroneServer is the Schema for the droneservers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DroneServerSpec defines the desired state of DroneServer.
            properties:
              githubServer:
                description: GithubServer is the DRONE_GITHUB_SERVER (e.g., https://github.com)
                type: string
              runnerCapacity:
                description: RunnerCapacity is the max concurrent pipelines per runner
                format: int32
                type: integer
              runnerReplicas:
                description: RunnerReplicas is the number of Drone runner replicas
                format: int32
                type: integer
              serverHost:
                description: ServerHost is the DRONE_SERVER_HOST (e.g., drone.example.com)
                type: string
            required:
            - githubServer
            - runnerCapacity
            - runnerReplicas
            - serverHost
            type: object
          status:
            description: DroneServerStatus defines the observed state of DroneServer
            properties:
              generatedSecret:
                description: GeneratedSecret is the auto-generated shared secret name
                type: string
              phase:
                description: Phase of the server (e.g., Running, Failed)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
